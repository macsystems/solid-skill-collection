---
name: skill-name
description: Use when <situation that should trigger this skill>. <One or two sentences
  describing what the skill does and the outcome it produces.>
# license: Apache-2.0        # optional; defaults to the repo license
# allowed-tools: Read, Grep  # optional; restrict the tools the skill may use
---

# Human-readable title

> Replace everything in this template. Keep the body lean — it is instructions to the
> agent, not documentation for people.

## When to use

Describe the concrete situations, requests, or code smells that should activate this
skill. This complements the `description` frontmatter.

## How to apply

Give the agent a clear, ordered procedure or a set of heuristics. For example:

1. Identify ...
2. Check for ...
3. Suggest ...

## Examples

Show a short before/after where the principle is violated and then fixed.

<!--
Optional: move long reference material into a separate file in this folder
(e.g. reference.md) and link to it here so this file stays focused.
-->
